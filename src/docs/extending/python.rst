Extend from Python
==================

Python integrations import native types::

   from miskeyed.workbench import WorkbenchWindow

Use Python to expose, compose, or host QObjects and widgets. Keep compilation,
dependency identity, resource lifetime, and rendering native. The Python example tool
is intentionally a composition example rather than a mirrored renderer.
