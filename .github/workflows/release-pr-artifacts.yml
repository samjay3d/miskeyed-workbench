name: release PR artifacts

# Release branches are stabilization surfaces. Reviewers need the exact candidate
# distributions, but review builds must have no publishing or repository-write access.
on:
    pull_request:
        branches:
            - "release/**"
    workflow_dispatch:

permissions:
    contents: read

jobs:
    build-review-artifacts:
        name: Build review artifacts
        uses: ./.github/workflows/build-distributions.yml
        permissions:
            contents: read
