version: 2
updates:
  # Keep the GitHub Actions pinned in the workflow files current (security + fixes).
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    commit-message:
      prefix: "ci"
